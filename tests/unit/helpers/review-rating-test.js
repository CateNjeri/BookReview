import { bookRating } from 'book-review/helpers/book-rating';
import { module, test } from 'qunit';

module('Unit | Helper | book rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bookRating([42]);
  assert.ok(result);
});
